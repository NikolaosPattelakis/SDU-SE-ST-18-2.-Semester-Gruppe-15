package smartsag.DTO.enums;

/**
 *
 * Holding the enums "citizen", "case", "department", "employee", "role" as types of DTO objects.
 */
public enum DTOType {
    CITIZEN, CASE, DEPARTMENT, EMPLOYEE, ROLE
}
