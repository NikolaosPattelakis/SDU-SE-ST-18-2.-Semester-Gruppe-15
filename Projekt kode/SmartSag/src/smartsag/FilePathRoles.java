
package smartsag;

/**
 * Interface that holds the file path of the XML file where roles are saved at.
 * 
 */
interface FilePathRoles {
   String FILE_PATH_ROLES_XML = "data/roles.xml"; 
}
