package Model.DAO.Interfaces;

import Smartsag.DTO.DTO;

/**
 *
 * An interface with the update method that can be used by a DAO.
 */
public interface UpdateInterface {
    
    public void update(DTO toUpdate);
}
