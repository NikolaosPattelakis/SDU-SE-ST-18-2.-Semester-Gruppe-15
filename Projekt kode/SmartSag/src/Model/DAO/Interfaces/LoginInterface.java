package Model.DAO.Interfaces;

/**
 * 
 * An interface with the login method that can be used by a DAO.
 */
public interface LoginInterface {
    
    public void login(String username, String password);
}
